/**
 * DEPRECATED — use lib/notifications/push-service.ts (canonical).
 * This shim re-exports the canonical service to avoid breaking any
 * remaining imports. Delete once all callers are migrated.
 */
export * from './push-service';
