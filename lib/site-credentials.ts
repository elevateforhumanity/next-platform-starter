/**
 * Canonical public credential count for marketing pages.
 * Derived from credentials catalog arrays — update when credentials/page.tsx changes.
 */
export const PUBLIC_CREDENTIALS_OFFERED = 22;
export const PUBLIC_CREDENTIALS_DISPLAY = `${PUBLIC_CREDENTIALS_OFFERED}+`;
