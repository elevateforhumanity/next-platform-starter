/**
 * Instructor portal layout — runs inside the admin shell.
 * Auth and nav are handled by app/admin/layout.tsx.
 * instructor and admin roles are both in adminRoles there.
 */
export default function InstructorLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
