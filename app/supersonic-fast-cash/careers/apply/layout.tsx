import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Careers Apply | Supersonic Fast Cash',
  description: 'Supersonic Fast Cash - Careers Apply for tax preparation services.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
