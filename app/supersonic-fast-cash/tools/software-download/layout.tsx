import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'SupersonicFastCash Download | Supersonic Fast Cash',
  description: 'Download SupersonicFastCash tax software for professional tax preparation.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
