/**
 * @deprecated Import from `@/lib/programs/static-registry` for new code.
 * Re-exports preserved so existing `@/data/programs/catalog` imports keep working.
 */
export {
  ALL_PROGRAMS,
  SECTORS,
  getProgramBySlug,
  getProgramsBySector,
  getStaticProgram,
} from '@/lib/programs/static-registry';
